{
  "name": "@vn-engine/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cmd /c \"set PATH=%USERPROFILE%\\.cargo\\bin;%PATH%&& tauri dev\"",
    "build": "cmd /c \"set PATH=%USERPROFILE%\\.cargo\\bin;%PATH%&& tauri build --no-bundle\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "typescript": "^5.8.3"
  }
}
